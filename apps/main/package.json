{
  "name": "@coki/main",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.cjs",
  "scripts": {
    "dev": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.cjs --external:electron --external:better-sqlite3 --external:jsdom && electron .",
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.cjs --external:electron --external:better-sqlite3 --external:jsdom"
  },
  "dependencies": {
    "@coki/engine": "workspace:*",
    "@coki/shared": "workspace:*",
    "better-sqlite3": "^12.0.0",
    "electron": "^32.0.0",
    "jsdom": "^26.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.0.0"
  }
}
